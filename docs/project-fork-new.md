# Project Fork New

## 88ffb31 - feat: make group panel resizable

本次提交优化了左侧分组栏目在长名称场景下的可读性，并允许用户按自己的屏幕空间调整分组栏宽度。

- 分组栏默认宽度从 `200px` 增加到 `240px`，`1200px` 和 `1024px` 断点下也分别同步增加 40px。
- 分组项和分组名称都新增 `title` 属性，名称被省略时可以悬停查看完整分组名。
- 收紧分组项左侧 padding、层级缩进和行内 gap，并补充 `min-width: 0` 等 flex 约束，避免长名称被计数和操作按钮过度挤压。
- 桌面端分组栏右边缘新增可拖拽宽度调整条，支持鼠标拖动和键盘左右箭头调整，调整后的宽度保存到浏览器本地。
- 折叠分组栏、移动端 drawer 和原有分组拖拽排序逻辑保持不变。
- 补充前端静态测试，覆盖 title、默认宽度、拖拽条和宽度持久化逻辑。

## upstream/main bd5d362 - merge upstream 2.1.0-2.7.0

本次将原作者 `upstream/main` 合并到 `dev`，同步上游 `2.1.0` 至 `2.7.0` 的功能与发布记录，并处理 fork 近期邮件搜索、转发轮询、本地保留和取信状态相关冲突。

- 同步邮箱分享功能：支持创建只读分享链接、分享管理、分享删除/批量操作，以及匿名分享页面读取邮箱邮件详情。
- 同步转发调度升级：轮询间隔改为秒级配置，新增串行/并行执行模式、并行 worker 数量配置和 `forwarding_run_lock` 防并发执行。
- 同步系统皮肤与主题能力：新增皮肤管理 API、皮肤示例、Editorial 皮肤、暗色/亮色主题切换，并让首页静态资源带内容哈希避免缓存旧资源。
- 同步账号列表偏好：搜索关键字、搜索范围、排序方式和标签筛选可持久化；账号搜索范围默认使用当前分组。
- 同步邮箱分享管理表格、账号标签悬停移除、编辑账号标签回显/保存、Outlook 上传账号与 Graph OAuth 自动提取测试指南。
- 同步正式账号“加入自动授权”入口，可将已有 Outlook 账号加入自动化授权队列，授权成功后覆盖账号授权字段但保留业务字段。
- 处理 `outlook_web/segments/08_forwarding_scheduler_errors.py` 冲突：以上游模块化转发任务为底座，保留 fork 的转发匹配规则、候选邮件隐藏入库、详情正文回填、账号取信状态记录和转发附加邮箱分组能力。
- 处理设置相关冲突：保留 fork 的验证码复制开关、普通邮箱本地保留自动展示新邮件、转发匹配规则设置，同时吸收上游皮肤设置、秒级转发轮询和并行 worker 设置。
- 处理前端冲突：保留 fork 的邮件内容搜索模式、排除条件、命中账号联动、分组栏折叠和取信状态筛选，同时吸收上游账号搜索偏好、标签可移除、导航栏和皮肤变量调整。
- 处理 `CHANGELOG.md` 冲突：保留上游 `2.1.0` 至 `2.7.0` 正式版本记录，并保留 fork 的 `2.0.72+1`、`2.0.72+2` 增量记录。
- 验证：`node --check` 覆盖核心前端脚本，`python -m py_compile` 覆盖冲突 Python 文件，`python -m pytest -q` 全量通过 `447 passed, 10 subtests passed`。

## 26028b1 - feat: add mail access status tracking

本次提交新增了账号级取信状态标记与筛选能力，用于区分永久性账号失效问题和临时取信失败问题，并帮助在账号列表中快速定位异常邮箱。

- 在 `accounts` 表新增 `mail_access_status`、`mail_access_reason`、`mail_access_error`、`mail_access_checked_at`、`mail_access_source` 字段，并补充索引与迁移逻辑。
- 新增统一的取信错误分类与状态写入逻辑，可识别 `AADSTS70000 / service abuse mode`、`invalid_grant`、认证失败、代理异常和网络超时等类型。
- 刷新 token、用户手动取信、邮件转发轮询候选拉取失败、邮件详情拉取失败都会更新账号取信状态；成功后会恢复为 `ok`。
- 普通邮箱 `folder=all` 失败时会保留 inbox / junkemail 下游 Graph、IMAP、OAuth 的详细错误，避免前端只看到“所有方式均失败”的粗略提示。
- `/api/accounts`、`/api/accounts/search` 和账号详情返回新增取信状态字段，并支持按 `mail_access_status` 服务端筛选。
- 账号列表新增取信状态标签、查看取信错误入口，以及默认折叠的取信状态筛选面板；展开后可筛选正常、失效邮箱、取信失败和未检查账号。
- 邮件内容搜索命中账号列表同步展示取信状态，保持和普通账号列表一致的卡片信息。
- 新增后端与前端测试，覆盖 schema、错误分类、状态写入、账号筛选、折叠筛选入口和转发失败写入状态等场景。

## 966a65b - feat: add forwarding match rules

本次提交新增了邮件转发轮询的匹配规则能力，用于在拉取详情和发送转发前按候选邮件摘要筛选。

- 在“邮件转发设置”新增“转发匹配规则”多行输入框，一行一条规则，大小写不敏感。
- 新增“同时匹配预览正文”开关；默认只匹配标题，开启后同时匹配 `body_preview`。
- 新增设置项 `forward_match_rules` 和 `forward_match_include_preview`，通过 `/api/settings` 读写并持久化到 SQLite `settings` 表。
- 转发轮询会在候选列表阶段应用规则，未命中的邮件不拉详情、不发送 SMTP/TG/企业微信转发，也不写转发日志。
- 空规则保持原有行为，不过滤候选邮件。
- 未命中邮件仍可推进游标，避免每轮重复扫描；但如果本轮已有命中邮件详情拉取或发送失败，后续未命中邮件不会把游标推进到失败邮件之后，保证失败邮件后续还能重试。
- 新增后端和前端测试，覆盖设置默认值与保存、标题匹配、预览匹配、多行 OR、大小写不敏感、未命中不发送，以及失败命中邮件的游标保护。

## upstream/main a19736a - merge upstream 2.0.72

本次将原作者 `upstream/main` 合并到 `dev`，同步上游 `2.0.70`、`2.0.71`、`2.0.72`，并处理分组管理前端脚本的冲突。

- 同步最多三级的树形分组能力：支持父分组选择、折叠展开、同级排序、跨层级拖拽移动，以及账号列表、账号搜索、分组导出和项目分组范围自动包含子分组账号。
- 同步 Cloudflare 临时邮箱工作流增强：批量生成、AI 用户名生成配置、显式用户名列表输入、成功后自动绑定标签，以及导入示例与渠道写法更新。
- 同步临时邮箱生成弹窗和设置页布局调整：Cloudflare、GPTMail、DuckMail 改为分段切换布局，并新增 DuckMail 密码显示/隐藏按钮。
- 同步相关后端、API 文档、浏览器扩展、OpenSpec 变更与测试用例，包括树形分组和 Cloudflare 临时邮箱新流程的覆盖。
- 处理 `static/js/index/02-groups.js` 冲突：保留 fork 的邮件搜索模式、排除条件和命中账号联动逻辑，同时吸收上游树形分组、分组折叠和 `persistGroupMove()` 相关实现。

## upstream/main a969cb7 - merge upstream 2.0.69

本次将原作者 `upstream/main` 合并到 `dev`，同步上游 `2.0.68`、`2.0.69`，并处理邮件详情 URL 构造的冲突。

- 同步普通邮箱本地保留详情的附件元数据回源逻辑：当缓存标记有附件但附件元数据为空时，会回退远程详情补齐附件并回填本地缓存。
- 同步 Microsoft Graph 附件元数据查询修复，避免选择不支持的 `contentId` 字段导致附件列表获取失败。
- 同步 Graph 邮件详情在附件元数据为空时仍保留 `has_attachments` 标记的修复，便于本地保留识别需要回源补齐的邮件。
- 同步普通邮箱详情请求稳定携带 `id_mode` 的修复，避免 Graph、UID 和 sequence 消息 ID 语义混用导致详情或附件读取失败。
- 处理 `static/js/index/05-emails.js` 冲突：保留 fork 邮件搜索结果跨账号打开详情的 `getEmailActionAccount(selectedEmail)` 逻辑，同时吸收上游 `appendEmailIdModeParam()` 参数传递。

## upstream/main e75181e - merge upstream 2.0.65

本次将原作者 `upstream/main` 合并到 `dev`，同步上游 `2.0.64`、`2.0.65` 和后续清理提交，并处理与 fork 更新日志的冲突。

- 同步 Cloudflare Temp Email 多渠道管理能力：支持多套 Worker、管理员密码、邮箱域名和独立邮件池，并迁移旧单渠道配置为默认渠道。
- 同步 Outlook OAuth 账号重新授权能力：编辑账号与刷新失败提示可重新授权已有账号，并通过 `POST /api/accounts/<account_id>/reauthorize` 更新授权字段。
- 同步上游 API 文档、OpenSpec 变更归档、前端弹窗和相关测试用例。
- 处理 `CHANGELOG.md` 冲突：保留上游 `2.0.64`、`2.0.65` 正式版本记录，同时保留 fork 的 `2.0.63+1` 至 `2.0.63+5` 增量记录。
- 保留 fork 的仓库与镜像地址配置，继续指向 `devmc12/outlook-email-manager`。

## 0a10d81 - feat: add conditional retained mail search

本次提交扩展了普通邮箱本地保留的邮件内容搜索能力，支持按账号级包含/排除条件筛用户，并优化搜索结果邮件与账号列表联动。

- 邮件搜索模式新增可展开的“排除条件”输入框，包含规则和排除规则都支持多行输入，一行一条规则。
- 包含规则按 OR 语义匹配，账号只要任一邮件命中任一包含规则即可进入候选。
- 排除规则按账号级生效，账号在同一分组、文件夹和本地保留范围内任一邮件命中排除规则即被排除。
- 支持只填写排除条件进行搜索，用于筛出“不包含某类邮件”的账号。
- 邮件搜索字段新增发件人，搜索范围包括标题、发件人、预览和去 HTML 后正文。
- 未选择账号时，右侧邮件列表显示全部命中邮件；点击命中账号后进入该账号的普通邮箱加载流程。
- 全部命中邮件列表中的“查看用户邮件”按钮会和“账号: xxx”放在同一行，并在点击后选中对应账号、同步滚动左侧账号列表到该用户。
- 邮件搜索命中账号列表保持原账号卡 UI、多选流程和更多操作菜单，只额外展示“X 封命中”。
- 刷新页面默认回到账号搜索模式，切换搜索 tab 时会同步清理右侧邮件列表状态。
- 补充后端与前端测试，覆盖包含/排除规则、排除-only 搜索、发件人匹配、原账号卡操作保留和账号滚动联动。

## 5b3ef45 - fix: restore mail search account actions

本次提交修复了邮件内容搜索后的命中账号列表 UI 和操作能力偏离原账号列表的问题。

- 邮件搜索命中账号卡恢复原账号列表样式，不再显示额外的 `M` 标识和“最近命中”文本。
- 命中账号卡只额外显示“X 封命中”标签，用于区分邮件内容搜索结果。
- 恢复账号卡右侧更多操作菜单，继续支持复制邮箱、转发日志、停用/启用、编辑和删除。
- 邮件搜索命中账号列表支持原有多选流程，包括 checkbox、多选模式、Shift 连选、拖选、全选和清空。
- 邮件搜索聚合账号补充 `forward_enabled`、别名、标签、备注、创建时间和刷新状态等字段，让批量操作和原卡片信息保持一致。
- 邮件搜索模式下的批量栏按普通账号处理，避免当前分组为临时邮箱时错误隐藏刷新、导出、转发、代理和移动等按钮。
- 补充接口与前端静态测试，覆盖命中账号字段、原 more 菜单、多选入口和命中数量标签。

## 8c025ac - feat: add retained mail content search

本次提交新增了基于普通邮箱本地保留数据库的邮件内容搜索能力，并保留账号列表联动。

- 搜索区新增“账号 / 邮件”分段切换，账号模式保持原有邮箱、别名、备注和标签搜索行为。
- 邮件模式调用 `GET /api/emails/search`，搜索 `retained_normal_mail_messages` 中的标题、预览和去 HTML 后的正文。
- 邮件搜索会包含普通列表缓存和转发轮询隐藏缓存，支持后续按标题、`body_preview` 和已缓存 `body` 检索邮件。
- 邮件模式左侧展示命中账号列表，包含账号邮箱、分组、命中数量和最近命中时间。
- 未选择账号时，右侧展示全部命中邮件；点击某个命中账号后只展示该账号命中的邮件，再次点击同一账号会取消筛选并恢复全部命中邮件。
- 搜索结果邮件会携带 `account_email`、`account_id`、`folder` 和 `id_mode`，打开详情、查看 raw 和下载附件时使用结果所属账号，不依赖当前普通邮箱上下文。
- 邮件搜索模式下隐藏跨账号批量邮件操作和详情删除入口，避免误触发不适合跨账号执行的操作。
- 修复邮件搜索参数中空格被编码为 `+` 后无法命中的问题，并保持移动端切换“邮件”标签时不关闭抽屉。
- 补充分组 select 桌面端紧凑宽度和移动端可点击尺寸样式。
- 新增后端与前端静态测试，覆盖标题、预览、正文搜索、账号聚合、过滤分页、隐藏缓存命中、跨账号详情请求和移动端抽屉行为。

## 5c6a2c1 - fix: improve multilingual verification code detection

本次提交加强了邮件列表验证码识别规则，修复部分多语言验证码邮件无法显示复制按钮的问题。

- 将验证码上下文从单条正则扩展为多语言规则列表，便于后续继续补充表达。
- 新增繁中、日文、韩文、意大利语、法语、阿语、西语、葡语和俄语验证码上下文。
- 修复日文 `一時検証コード`、`検証コード` 等邮件无法识别验证码的问题。
- 新增全角数字、阿拉伯-印度数字和波斯数字归一化，复制时输出 ASCII 数字。
- 保持 4-8 位数字或字母数字候选码规则，并继续规避年份、账号数字和无验证码语义的普通数字。
- 新增多语言验证码提取测试，覆盖 zh-TW、ja、ko、it、fr、ar、es、pt、ru 等样例。

## 61f0121 - feat: retain forwarded poll emails

本次提交新增了邮件转发轮询写入普通邮箱本地保留的能力。

- 在 `retained_normal_mail_messages` 新增 `forward_poll_cached` 和 `forward_poll_cached_at` 字段，用于区分“转发轮询已缓存”和“普通列表已展示/确认”。
- 普通邮箱本地保留开启时，转发轮询会把候选邮件的标题、发件人、收件人、时间和 `body_preview` 写入隐藏缓存。
- 转发阶段成功取到邮件详情后，会把正文、正文类型、抄送和附件元数据补写到同一条保留记录。
- 轮询缓存的新邮件默认不提前出现在普通本地列表中；用户打开邮箱后仍按原有“点击显示”或“自动展示新邮件”逻辑处理。
- 本地搜索带标题、发件人或关键词条件时，会同时搜索普通列表缓存和转发轮询隐藏缓存，支持后续基于标题、预览和已缓存正文检索。
- 新增回归测试覆盖 schema 迁移、轮询写库、保留关闭不写库、写库失败不影响转发、新邮件提示保持以及搜索可见性。

## 227d387 - feat: add retained new mail auto show

本次提交新增了普通邮箱本地保留的新邮件自动展示能力。

- 在“普通邮箱本地保留”设置栏中，将“自动展示新邮件”开关放在“启用普通邮箱本地保留”右侧。
- 新增设置项 `normal_mail_local_retention_auto_show_new_mail`，默认关闭，并通过 `/api/settings` 读写持久化。
- 首页启动时会随 `/api/settings` 同步该开关，保证刷新页面后仍按用户选择处理新邮件提示。
- 默认行为保持不变：后台同步发现新邮件后展示“点击显示”提示条，用户点击后再合并新邮件。
- 开启后，后台同步发现新邮件会自动调用待处理同步合并逻辑，把新邮件加入当前列表，并继续触发高亮、列表缓存更新和正文保留补齐。
- 同步更新本地保留说明、API 文档和项目 map，并补充设置 roundtrip、前端接线与 Windows SQLite 临时库关闭测试。

## b7ec622 - feat: add verification code copy toggle

本次提交新增了邮件列表验证码识别与复制能力。

- 邮件列表会基于已有的 `subject`、`body_preview` 和已随列表返回的 `body` 识别验证码，不额外请求邮件详情，也不新增数据库表。
- 命中验证码时，在邮件标题右侧显示蓝色验证码按钮，按钮文本就是验证码本身。
- 点击验证码按钮会复制验证码，并阻止邮件列表行的默认打开行为。
- 验证码识别优先匹配验证码、登录代码、security code、verification code、OTP 等上下文，支持 4-8 位数字或字母数字验证码，并规避年份、账号数字和无验证码语义的普通数字。
- 常规设置新增“开启复制验证码”开关，默认开启；关闭后前端不再进行验证码识别，也不渲染复制按钮。
- 新增前端与接口测试，覆盖验证码提取、误报规避、设置默认值、设置保存、委托点击和移动端样式约束。

## ff21d5e - feat: add collapsible group panel

本次提交新增了 Web 首页分组面板的桌面端折叠能力。

- 在“分组面板”右侧垂直中部新增窄折叠条，仅在悬停分组面板或键盘聚焦时显示，减少对分组列表的遮挡。
- 点击折叠条后，分组面板通过宽度动画收起，并隐藏面板内容与边框占位。
- 分组面板收起后，屏幕左侧垂直中部显示贴边展开条，点击后通过动画恢复分组面板。
- 折叠状态会写入 `localStorage`，桌面端刷新后保持用户上次选择；切换到移动端时自动禁用桌面折叠状态。
- 响应式宽度同步使用 `--group-panel-width`，避免 1200px / 1024px 桌面断点下折叠条错位。
- 增加前端静态测试，覆盖控件存在、桌面端动画样式、响应式宽度变量和折叠状态初始化逻辑。

## a89b0a3 - feat: add forwarding group labels and project map

本次提交新增了邮件转发的“附加邮箱分组”能力。

- 在“邮件转发设置”中新增“附加邮箱分组”开关，默认关闭。
- 新增设置项 `forward_include_account_group`，通过设置 API 读写并持久化到 SQLite `settings` 表。
- 转发轮询读取账号时关联 `groups` 表，拿到账号所属分组名。
- 开启后，Telegram 和企业微信转发文本会在时间后追加 `分组: xxx`。
- 开启后，SMTP 转发只在转发邮件的顶部元信息区追加分组行，不修改原邮件正文内容。
- 增加转发相关测试，覆盖默认关闭不输出分组、开启后输出账号分组的行为。
- 补充 `docs/project-map.md`，记录项目结构、技术栈、数据库表和二开注意点。
